# Copy to .env and fill in. The anon key is the publishable (read-only) key.
# RLS must keep the anon role limited to SELECT on the clean_quotes view.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
